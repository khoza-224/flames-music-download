services:
  - type: web
    name: flames-music-download
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: JAMENDO_CLIENT_ID
        sync: false
